import React from "react";
import { useCookies } from "react-cookie";

const useCookieByName = ({cookieName}) => {


  if(cookie){

  }else{

  }

  return (
    <div className="App">
      <h1>React cookies</h1>
      <p>{cookies}</p>
    </div>
  );
};


export default useCookieByName;